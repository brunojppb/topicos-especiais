<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      O formulário contem <%= pluralize(object.errors.count, "erro") %>:
      <ul>
        <% object.errors.full_messages.each do |msg| %>
          <li class="error_li"> <%= msg %> </li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>
